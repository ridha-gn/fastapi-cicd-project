name: Simple Test

on: [push]

jobs:
  simple-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Success message
        run: echo "🎉 GitHub Actions is working! Your PFE project is set up correctly."
